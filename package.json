{
  "name": "equinox-logger-2",
  "version": "2.1.59",
  "private": true,
  "description": "Thread safety C++ logger with file and console output support.",
  "scripts": {
    "hooks:install": "sh ./scripts/install_hooks.sh",
    "release-notes:generate": "sh ./scripts/generate_release_history.sh",
    "readme:sync-version": "python3 ./scripts/sync_readme_version.py"
  }
}
